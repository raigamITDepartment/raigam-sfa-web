name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - development
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - development

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    env:
      # 🌐 Environment Variables (use secrets for sensitive data)
      VITE_API_BASE_URL: ${{ secrets.VITE_API_BASE_URL }}
      VITE_FIREBASE_API_KEY: ${{ vars.VITE_FIREBASE_API_KEY }}
      VITE_FIREBASE_AUTH_DOMAIN: ${{ vars.VITE_FIREBASE_AUTH_DOMAIN }}
      VITE_FIREBASE_PROJECT_ID: ${{ vars.VITE_FIREBASE_PROJECT_ID }}
      VITE_FIREBASE_STORAGE_BUCKET: ${{ vars.VITE_FIREBASE_STORAGE_BUCKET }}
      VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ vars.VITE_FIREBASE_MESSAGING_SENDER_ID }}
      VITE_FIREBASE_APP_ID: ${{ vars.VITE_FIREBASE_APP_ID }}
      VITE_FIREBASE_MEASUREMENT_ID: ${{ vars.VITE_FIREBASE_MEASUREMENT_ID }}
      VITE_FIREBASE_LOGIN_DOMAIN: ${{ vars.VITE_FIREBASE_LOGIN_DOMAIN }}
      VITE_FIREBASE_LOGIN_EMAIL: ${{ vars.VITE_FIREBASE_LOGIN_EMAIL }}
      VITE_FIREBASE_LOGIN_PASSWORD: ${{ secrets.VITE_FIREBASE_LOGIN_PASSWORD }}
      VITE_GOOGLE_MAPS_API_KEY: ${{ vars.VITE_GOOGLE_MAPS_API_KEY }}
      VITE_FORM_BUILDER_BLOB_CONTAINER: ${{vars.VITE_FORM_BUILDER_BLOB_CONTAINER}}
      VITE_FORM_BUILDER_BLOB_ENDPOINT: ${{vars.VITE_FORM_BUILDER_BLOB_ENDPOINT}}
      VITE_FORM_BUILDER_BLOB_SAS_TOKEN: ${{vars.VITE_FORM_BUILDER_BLOB_SAS_TOKEN}}
      VITE_FORM_BUILDER_STORAGE_CONNECTION_STRING: ${{vars.VITE_FORM_BUILDER_STORAGE_CONNECTION_STRING}}
    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4
        with:
          submodules: true
          lfs: false

      - name: ⚙️ Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22.13.0' 
          cache: 'npm'

      - name: 📦 Install Dependencies
        run: npm ci

      - name: 🏗️ Build Vite App
        run: npm run build

      - name: 🚀 Deploy to Azure Static Web Apps
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_BLUE_SEA_0FCBB0D00 }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          ###### Repository/Build Configurations ######
          app_location: "/"            # Root of the app
          api_location: ""             # No API folder (optional)
          output_location: "build"      # ✅ Vite build output folder
          ###### End Configurations ######

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: 🧹 Close Pull Request
        id: closepullrequest
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_BLUE_SEA_0FCBB0D00 }}
          action: "close"
